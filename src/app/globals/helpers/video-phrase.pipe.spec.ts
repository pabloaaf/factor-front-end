import { VideoPhrasePipe } from './video-phrase.pipe';

describe('VideoPhrasePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoPhrasePipe();
    expect(pipe).toBeTruthy();
  });
});
